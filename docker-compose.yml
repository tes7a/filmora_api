services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: filmora
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5433:5432'
    volumes:
      - filmora_pg:/var/lib/postgresql/data

volumes:
  filmora_pg:
